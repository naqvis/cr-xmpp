name: cr-xmpp
version: 0.2.0
description: |
  XMPP/Jabber Library for Crystal focusing on simplicity, simple automation, and IoT.

authors:
  - Ali Naqvi <syed.alinaqvi@gmail.com>

crystal: ">= 0.36.0, < 2.0.0"

dependencies:
  openssl_ext:
    github: spider-gazelle/openssl_ext
    # Required for channel binding support (RFC 5929, RFC 9266)
    # Provides extended OpenSSL functionality including certificate DER encoding

license: MIT
